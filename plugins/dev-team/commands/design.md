---
name: design
description: Run the UI Designer agent to create component specs, layouts, and visual design tokens.
argument-hint: "<task description>"
---

Use the `ui-designer` agent to design the user interface for the following task. Produce component specifications, responsive layouts, design tokens, and accessibility requirements.

Check `specs/ux-research.md` for existing UX research to inform the design.

Task: $ARGUMENTS
